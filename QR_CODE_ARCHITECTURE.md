# QR Code Feature - Architecture & Workflows

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    USER INTERFACE (React)                   │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Issue Certificate          Verify Certificate              │
│  ┌─────────────────┐       ┌──────────────────────┐         │
│  │ CertificateForm │       │ VerifyCertificate    │         │
│  │  - Form inputs  │       │  ┌────────────────┐  │         │
│  │  - QR Display ✨│       │  │QRCodeVerif..✨ │  │         │
│  │  - Success msg  │       │  ├────────────────┤  │         │
│  └────────┬────────┘       │  │Camera Scanning │  │         │
│           │                │  │Image Upload    │  │         │
│           │                │  │Manual Entry    │  │         │
│           │                │  └────────────────┘  │         │
│           └────────┬───────┴──────────────┬───────┘         │
│                    │                      │                 │
└────────────────────┼──────────────────────┼─────────────────┘
                     │                      │
        HTTP API Calls via certificateService
                     │                      │
┌────────────────────┼──────────────────────┼─────────────────┐
│         Django REST API (Port 8000)       │                 │
├────────────────────┼──────────────────────┼─────────────────┤
│                    ▼                      ▼                 │
│  POST /api/certificates/issue/    POST /api/certificates/  │
│  ┌──────────────────────┐          verify-qr/             │
│  │ IssueCertificateView │          ┌────────────────────┐  │
│  │ ✨ Generate QR Code  │          │verify_by_qr_code() │  │
│  │ ✨ Save to DB        │          │ - Decode QR        │  │
│  │ - Blockchain call    │          │ - Extract hash     │  │
│  │ - Create Certificate │          │ - Verify on chain  │  │
│  └──────────────────────┘          └────────────────────┘  │
│           │                                  │               │
└───────────┼──────────────────────────────────┼───────────────┘
            │                                  │
            ▼                                  ▼
      ┌──────────────────────────────────────────────┐
      │      Backend Processing Layer               │
      ├──────────────────────────────────────────────┤
      │                                              │
      │  ┌──────────────────────────────────────┐   │
      │  │   qr_generator.py                    │   │
      │  │  ┌────────────────────────────────┐  │   │
      │  │  │ generate_qr_code()             │  │   │
      │  │  │  - Input: cert_hash            │  │   │
      │  │  │  - Create QR with URL          │  │   │
      │  │  │  - Convert to PNG image        │  │   │
      │  │  │  - Return ContentFile          │  │   │
      │  │  └────────────────────────────────┘  │   │
      │  │  ┌────────────────────────────────┐  │   │
      │  │  │ decode_qr_code_hash()          │  │   │
      │  │  │  - Input: image_file           │  │   │
      │  │  │  - Decode QR with pyzbar       │  │   │
      │  │  │  - Extract hash from URL       │  │   │
      │  │  │  - Return hash string          │  │   │
      │  │  └────────────────────────────────┘  │   │
      │  └──────────────────────────────────────┘   │
      │                                              │
      │  ┌──────────────────────────────────────┐   │
      │  │   Database Integration              │   │
      │  │  ┌────────────────────────────────┐  │   │
      │  │  │ Certificate Model              │  │   │
      │  │  │  - student_name                │  │   │
      │  │  │  - course                      │  │   │
      │  │  │  - institution                 │  │   │
      │  │  │  - cert_hash (unique)          │  │   │
      │  │  │  - qr_code (ImageField) ✨     │  │   │
      │  │  └────────────────────────────────┘  │   │
      │  └──────────────────────────────────────┘   │
      │                                              │
      │  ┌──────────────────────────────────────┐   │
      │  │   Blockchain Integration            │   │
      │  │  ┌────────────────────────────────┐  │   │
      │  │  │ issue_certificate()            │  │   │
      │  │  │  - Hash generation             │  │   │
      │  │  │  - Blockchain submission       │  │   │
      │  │  │  - Event extraction            │  │   │
      │  │  │  - Return cert_hash            │  │   │
      │  │  └────────────────────────────────┘  │   │
      │  │  ┌────────────────────────────────┐  │   │
      │  │  │ verify_certificate_on_chain()  │  │   │
      │  │  │  - Blockchain query            │  │   │
      │  │  │  - Hash validation             │  │   │
      │  │  │  - Return is_valid             │  │   │
      │  │  └────────────────────────────────┘  │   │
      │  └──────────────────────────────────────┘   │
      │                                              │
      └──────────────────────────────────────────────┘
            │                          │
            ▼                          ▼
      ┌─────────────────┐    ┌──────────────────────┐
      │  SQLite DB      │    │  Ganache Blockchain  │
      │  ┌───────────┐  │    │  ┌────────────────┐  │
      │  │Cert Table │  │    │  │Smart Contract  │  │
      │  │+ QR Code  │  │    │  │CertificateXYZ  │  │
      │  └───────────┘  │    │  └────────────────┘  │
      │  ┌───────────┐  │    │  ┌────────────────┐  │
      │  │Media Dir  │  │    │  │Stored Hashes   │  │
      │  │/qr_codes/ │  │    │  │+ Metadata      │  │
      │  └───────────┘  │    │  └────────────────┘  │
      └─────────────────┘    └──────────────────────┘
```

---

## 📱 QR Code Verification Workflow

### Flow 1: Certificate Issuance with QR Code Generation

```
┌─────────────────────────────────────────────────────────────┐
│                     USER: ISSUE CERTIFICATE                 │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 1. Admin fills certificate form       │
        │    - Student name                     │
        │    - Course                           │
        │    - Institution                      │
        │    - Issue date                       │
        │    - PDF certificate                 │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 2. Frontend sends POST request         │
        │    POST /api/certificates/issue/      │
        │    FormData with all fields           │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 3. Backend processes issuance         │
        │    - Validates input                  │
        │    - Calls blockchain                 │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 4. Smart contract issues certificate  │
        │    - Stores hash                      │
        │    - Emits event                      │
        │    - Returns tx_hash                  │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 5. ✨ QR Code Generation ✨          │
        │    - generate_qr_code(cert_hash)     │
        │    - Creates PNG image with URL      │
        │    - Returns ContentFile             │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 6. Save to Database                   │
        │    - Create Certificate record        │
        │    - Attach QR code image             │
        │    - Save to media/qr_codes/          │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 7. Return API Response                │
        │    {                                  │
        │      cert_hash: "0x...",              │
        │      qr_code_url: "/media/qr_...",   │ ← NEW!
        │      transaction_hash: "0x...",       │
        │      certificate: {...}              │
        │    }                                  │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 8. Frontend Display Success Page      │
        │    - Show certificate details        │
        │    - Display QR code image ✨        │
        │    - Show instructions                │
        │    - Provide download/print options   │
        └───────────────────────────────────────┘
```

### Flow 2: Camera Scanning Verification

```
┌─────────────────────────────────────────────────────────────┐
│              USER: VERIFY WITH CAMERA SCAN                  │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 1. User opens Verify page             │
        │    - Sees QR Code tab                 │
        │    - Clicks "Scan with Camera"        │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 2. Browser requests camera permission │
        │    - User grants access               │
        │    - Camera stream starts             │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 3. ✨ Real-time QR Scanning ✨      │
        │    jsQR library running               │
        │    - requestAnimationFrame loop       │
        │    - Canvas captures video frame      │
        │    - Analyzes for QR codes            │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 4. QR Code Detected!                  │
        │    - jsQR library returns data        │
        │    - Extracts URL from QR             │
        │    - Parses cert_hash                 │
        │    - Stops camera                     │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 5. Send to Backend for Verification   │
        │    GET /api/certificates/verify/..../│
        │    Parameters: {                      │
        │      cert_hash: decoded_from_qr      │
        │    }                                  │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 6. Backend Verification               │
        │    - Find certificate in DB           │
        │    - Call blockchain verification     │
        │    - Return is_valid status           │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 7. Display Results                    │
        │    - Green checkmark if VALID ✅     │
        │    - Red X if INVALID ❌              │
        │    - Certificate details              │
        │    - Blockchain confirmation          │
        └───────────────────────────────────────┘
```

### Flow 3: Image Upload Verification

```
┌─────────────────────────────────────────────────────────────┐
│             USER: VERIFY WITH IMAGE UPLOAD                  │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 1. User opens Verify page             │
        │    - Sees QR Code tab                 │
        │    - Clicks "Upload QR Image"         │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 2. Drag & Drop QR Image               │
        │    Option A: Drag file into area      │
        │    Option B: Click to browse & select │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 3. Send to Backend                    │
        │    POST /api/certificates/verify-qr/ │
        │    FormData: {                        │
        │      qr_image: File                   │
        │    }                                  │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 4. Backend QR Decoding                │
        │    - Receive image file               │
        │    - decode_qr_code_hash(image)       │
        │    - pyzbar decodes QR                │
        │    - Extract URL/hash                 │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 5. Backend Verification               │
        │    - Find certificate in DB           │
        │    - Call blockchain verification     │
        │    - Return is_valid status           │
        └───────────────────────────────────────┘
                            │
                            ▼
        ┌───────────────────────────────────────┐
        │ 6. Display Results                    │
        │    - Green checkmark if VALID ✅     │
        │    - Red X if INVALID ❌              │
        │    - Certificate details              │
        │    - Blockchain confirmation          │
        └───────────────────────────────────────┘
```

---

## 🗂️ Component Interaction Diagram

```
┌──────────────────────────────────────────────────────────────────┐
│                      FRONTEND (React)                             │
├──────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌─────────────────────┐         ┌──────────────────────────┐   │
│  │ CertificateForm     │         │ VerifyCertificate        │   │
│  ├─────────────────────┤         ├──────────────────────────┤   │
│  │                     │         │                          │   │
│  │ Form Inputs         │         │ ┌────────────────────┐   │   │
│  │  ├─ Student Name    │         │ │QRCodeVerification  │   │   │
│  │  ├─ Course          │         │ │ ├─ Camera Scan     │   │   │
│  │  ├─ Institution     │         │ │ ├─ Image Upload    │   │   │
│  │  ├─ Issue Date      │         │ │ └─ Manual Entry    │   │   │
│  │  └─ PDF File        │         │ └────────────────────┘   │   │
│  │                     │         │                          │   │
│  │ On Submit:          │         │ CertificateVerification  │   │
│  │ Call API ──────────────────────────                      │   │
│  │ certificateService  │         │                          │   │
│  │ .issueCertificate() │         └──────────────────────────┘   │
│  │                     │                                         │
│  │ On Success:         │                                         │
│  │ Display QR Code ✨  │                                         │
│  │ (from response)     │                                         │
│  └─────────────────────┘                                         │
│                                                                    │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │          certificateService.js (API Client)                │ │
│  ├─────────────────────────────────────────────────────────────┤ │
│  │                                                             │ │
│  │ export functions:                                          │ │
│  │  ├─ issueCertificate(data)   → POST /issue/               │ │
│  │  ├─ verifyCertificate(hash)  → GET /verify/{hash}/        │ │
│  │  └─ verifyByQRCode()         → POST /verify-qr/           │ │
│  │                                                             │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                    │
│  External Libraries:                                             │
│  ├─ jsQR: Real-time QR scanning from camera                    │
│  ├─ Material-UI: Beautiful components                          │
│  └─ React: Component framework                                 │
│                                                                    │
└──────────────────────────────────────────────────────────────────┘
                              │ ▲
                 HTTP Requests │ │ JSON Responses
                              ▼ │
┌──────────────────────────────────────────────────────────────────┐
│                      BACKEND (Django)                             │
├──────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ REST API Endpoints (views.py)                            │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │                                                          │   │
│  │ POST /api/certificates/issue/                           │   │
│  │  └─ IssueCertificateView.post()                          │   │
│  │     ├─ Validate input                                    │   │
│  │     ├─ Call blockchain.issue_certificate()              │   │
│  │     ├─ generate_qr_code() ✨                            │   │
│  │     ├─ Create Certificate with QR                       │   │
│  │     └─ Return response with qr_code_url                 │   │
│  │                                                          │   │
│  │ GET /api/certificates/verify/{hash}/                    │   │
│  │  └─ verify_certificate_view()                           │   │
│  │     ├─ Get certificate from DB                          │   │
│  │     ├─ Call blockchain.verify_certificate_on_chain()    │   │
│  │     └─ Return results                                    │   │
│  │                                                          │   │
│  │ POST /api/certificates/verify-qr/ ✨ NEW               │   │
│  │  └─ verify_by_qr_code()                                │   │
│  │     ├─ Decode QR image                                  │   │
│  │     ├─ Extract hash                                     │   │
│  │     ├─ Verify on blockchain                            │   │
│  │     └─ Return results                                    │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ QR Code Processing (qr_generator.py)                     │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │                                                          │   │
│  │ generate_qr_code(cert_hash)                              │   │
│  │  ├─ Create QRCode object                                │   │
│  │  ├─ Encode verification URL                             │   │
│  │  ├─ Convert to PNG using Pillow                         │   │
│  │  └─ Return ContentFile                                  │   │
│  │                                                          │   │
│  │ decode_qr_code_hash(image_file)                          │   │
│  │  ├─ Use pyzbar to decode                                │   │
│  │  ├─ Extract URL from QR data                            │   │
│  │  ├─ Parse certificate hash                              │   │
│  │  └─ Return hash string                                  │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ Database (models.py)                                     │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │                                                          │   │
│  │ class Certificate(Model):                                │   │
│  │  ├─ student_name: CharField                             │   │
│  │  ├─ course: CharField                                   │   │
│  │  ├─ institution: CharField                              │   │
│  │  ├─ cert_hash: CharField (unique)                       │   │
│  │  ├─ qr_code: ImageField ✨ NEW                         │   │
│  │  ├─ is_revoked: BooleanField                            │   │
│  │  └─ created_at: DateTimeField                           │   │
│  │                                                          │   │
│  │ Media Storage:                                           │   │
│  │  └─ /media/qr_codes/qr_0x{hash}.png                    │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ Blockchain Integration (blockchain.py)                   │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │                                                          │   │
│  │ issue_certificate()                                      │   │
│  │  ├─ Generate keccak256 hash                              │   │
│  │  ├─ Submit to smart contract                            │   │
│  │  ├─ Extract hash from event                             │   │
│  │  └─ Return cert_hash                                    │   │
│  │                                                          │   │
│  │ verify_certificate_on_chain()                            │   │
│  │  ├─ Query smart contract                                │   │
│  │  ├─ Check if hash exists                                │   │
│  │  └─ Return is_valid status                              │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                    │
└──────────────────────────────────────────────────────────────────┘
                              │ ▲
            Blockchain Calls  │ │ Events/Queries
                              ▼ │
┌──────────────────────────────────────────────────────────────────┐
│              Blockchain (Ganache)                                 │
├──────────────────────────────────────────────────────────────────┤
│                                                                    │
│ Smart Contract: CertificateVerification                           │
│  ├─ issueCertificate(student, course, institution, date)         │
│  │  └─ Generates and stores hash                                 │
│  │     Emits: CertificateIssued event with hash                 │
│  │                                                               │
│  ├─ verifyCertificate(certHash)                                 │
│  │  └─ Returns: (is_valid, details)                             │
│  │                                                               │
│  └─ revokeCertificate(certHash)                                 │
│     └─ Marks certificate as revoked                             │
│                                                                    │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagram

```
Certificate Issuance Data Flow:

User Input Form
    ↓
{studentName, course, institution, issueDate, pdf}
    ↓
Frontend: certificateService.issueCertificate()
    ↓
POST /api/certificates/issue/
    ↓
Backend: IssueCertificateView.post()
    ├─ Validate input
    ├─ Generate issue_date timestamp (UTC midnight)
    ├─ Call blockchain.issue_certificate()
    │  ├─ Keccak256(student, course, institution, timestamp)
    │  ├─ Submit to smart contract
    │  ├─ Wait for transaction
    │  └─ Extract event hash
    │
    ├─ ✨ generate_qr_code(cert_hash)
    │  ├─ Create QR with verification URL
    │  ├─ Encode to PNG
    │  └─ Return ContentFile
    │
    ├─ Create Certificate instance
    ├─ Attach QR code image
    ├─ Save to database
    │
    └─ Return JSON response
        ├─ cert_hash
        ├─ transaction_hash
        ├─ qr_code_url ✨
        └─ certificate details
            ↓
        Frontend: Display success page with QR code


Certificate Verification (QR Scan) Data Flow:

User Points Camera at QR Code
    ↓
jsQR Decodes QR → URL
    ↓
Extract cert_hash from URL
    ↓
Frontend: certificateService.verifyCertificate(cert_hash)
    ↓
GET /api/certificates/verify/{cert_hash}/
    ↓
Backend: verify_certificate_view()
    ├─ Get Certificate from database
    ├─ Call blockchain.verify_certificate_on_chain()
    │  ├─ Query smart contract
    │  ├─ Check if hash exists
    │  └─ Return (is_valid, details)
    │
    └─ Return JSON response
        ├─ certificate details
        ├─ blockchain_verification
        └─ is_valid status
            ↓
        Frontend: Display results


Certificate Verification (Image Upload) Data Flow:

User Uploads QR Image
    ↓
POST /api/certificates/verify-qr/
    ↓
Backend: verify_by_qr_code()
    ├─ Receive image file
    ├─ decode_qr_code_hash(image_file)
    │  ├─ pyzbar decodes QR
    │  ├─ Extract URL
    │  └─ Parse cert_hash
    │
    ├─ Get Certificate from database
    ├─ Call blockchain.verify_certificate_on_chain()
    │  ├─ Query smart contract
    │  └─ Return is_valid status
    │
    └─ Return JSON response
        ├─ certificate details
        ├─ blockchain_verification
        └─ is_valid status
            ↓
        Frontend: Display results
```

---

This architecture ensures:
✅ Clean separation of concerns
✅ Scalable and maintainable code
✅ Multiple verification methods
✅ Full blockchain integration
✅ User-friendly interface
✅ Secure and reliable QR handling
